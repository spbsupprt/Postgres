host    all             all             127.0.0.1/32            scram-sha-256
host    replication     replicator      {{ slave_ip }}/32        scram-sha-256
host    all             barman          {{ barman_ip }}/32       scram-sha-256
host    replication     barman          {{ barman_ip }}/32       scram-sha-256
